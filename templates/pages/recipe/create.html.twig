{% extends 'base.html.twig' %}

{% block title %}Créer une recette{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Créer une recette</h1>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-warning mt-3">Valider</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
