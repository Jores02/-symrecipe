{% extends 'base.html.twig' %}

{% block title %}Modifier la recette{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Modifier la recette : {{ recipe.name }}</h1>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-success mt-3">Enregistrer</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
